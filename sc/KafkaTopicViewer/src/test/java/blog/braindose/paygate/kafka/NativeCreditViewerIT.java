package blog.braindose.paygate.kafka;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeCreditViewerIT extends CreditViewerTest {

    // Execute the same tests but in native mode.
}